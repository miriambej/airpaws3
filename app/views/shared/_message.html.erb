<% unless flash.empty? %>
  <script type="text/javascript">
    <% flash.each do |key, value| %>
      <% type = key.to_s.gsub('alert','error').gsub('notice','success') %>
      toastr['<%= type %>']('<%= value %>')
      <% end %>
  </script>
<% end %>

<%-#  what is doing in line 4 i.e flash[:notice] = "Sign in successfully" as alert and error does not exist -%>
<%- # what is doing in line 5 i.e toastr['error']('Full name can't be blank) that is what we are going to transform-%>
